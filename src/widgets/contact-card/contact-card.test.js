import ContactCardWidget from './contact-card.widget';

describe('Contact card widget Test :: -> ', function () {
    const component = new ContactCardWidget();

    it('Should create widget ** ->', function () {
        expect(component).toBeTruthy();
    });
});